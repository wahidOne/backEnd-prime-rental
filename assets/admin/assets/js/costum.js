// const hasSubMenu = [...document.querySelectorAll(".has-sub-menu")];

// hasSubMenu.map((submenu) => {
// 	const subMenuLink = submenu.querySelector(".sub-menu-link");
// 	submenu.addEventListener("click", (e) => {
// 		e.preventDefault();

// 		if (e.target.classList.contains(".sub-menu-link")) {
// 			console.log("true");
// 		}
// 	});

// 	if (submenu.classList.contains("active")) {
// 		const headerSubMenu = [
// 			...submenu.querySelectorAll(".side-header-sub-menu"),
// 		];
// 		headerSubMenu.map((element) => {
// 			element.classList.add("show");
// 		});
// 	}
// });
